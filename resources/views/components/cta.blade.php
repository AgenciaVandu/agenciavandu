<section class="cta-section">
    <div class="container">
        <div class="cta_contenedor">
            {{$titulocta ?? ''}}
            <a href="{{route('cotizar')}}" class="btn-cta">Recibir una propuesta</a>
        </div>
    </div>
</section>